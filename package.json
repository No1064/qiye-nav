{
  "name": "qiye",
  "version": "0.2.0",
  "private": true,
  "description": "Self-hosted bookmarks, browser start page, and AI-assisted organization",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "setup": "npm --prefix services/ingest ci",
    "sync": "node scripts/sync-newtab-home.mjs",
    "check": "node scripts/sync-newtab-home.mjs --check && npm --prefix services/ingest run build && node scripts/check-release.mjs",
    "test": "npm --prefix services/ingest test && npm --prefix services/ingest/public/manage test && npm --prefix services/ingest/public/home test && npm --prefix extension test",
    "build": "npm run sync && npm --prefix services/ingest run build",
    "dev": "node scripts/dev.mjs",
    "release:source": "python3 scripts/package-release.py",
    "release:images": "bash scripts/build-images.sh"
  }
}
