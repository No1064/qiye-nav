# 安全政策

当前维护版本为最新的 0.2.x；建议升级到最新补丁版本。

请通过 [GitHub 私密漏洞报告](https://github.com/No1064/qiye-nav/security/advisories/new) 报告漏洞。若入口不可用，请先创建不包含利用细节的 Issue，请求私密联系渠道。不要在公开 Issue 中上传 `.env`、目录数据、密码哈希、令牌或 AI 配置。

## 部署边界

- 首页和公开目录 API 可供未登录用户读取。不要把包含私人链接的实例直接暴露到公网；需要私有访问时使用 VPN 或在反向代理层增加访问认证。
- 管理员没有默认密码。首次部署通过交互脚本设置，后台「账号安全」可修改密码；修改后所有管理会话失效。
- 扩展令牌拥有写入权限。浏览器扩展会在用户授权的服务地址使用该令牌。
- 远程元数据、网站图标及 AI 请求会访问外部服务；AI 整理会向所配置提供商发送选中的书签内容。
- 开启 HTTPS 后设置 `ADMIN_COOKIE_SECURE=true`，按需限制 CORS 来源。不要开放 Docker 守护进程。
- 备份时同时保存数据和 AI 加密密钥，否则加密的提供商配置无法恢复。备份也应限制访问。
